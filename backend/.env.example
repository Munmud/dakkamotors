# Copy to .env for local development. Defaults below are safe for local work only.
SECRET_KEY=dev-only-insecure-key-do-not-use-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# No database URL: there is no relational database. The store talks to DynamoDB Local
# (docker compose up -d dynamodb) and identity to Cognito.
# DYNAMODB_ENDPOINT_URL=http://localhost:8123

# Leave AWS_STORAGE_BUCKET_NAME unset locally to store uploads on disk under mediafiles/.
# AWS_STORAGE_BUCKET_NAME=dakkamotors-backend-media
# AWS_S3_REGION_NAME=ap-northeast-1
# MEDIA_CUSTOM_DOMAIN=dakkamotors.com

# Used once by `manage.py create_admin_user` to seed the production superuser.
# DJANGO_ADMIN_USERNAME=admin
# DJANGO_ADMIN_EMAIL=admin@dakkamotors.com
# DJANGO_ADMIN_PASSWORD=

# ---- DynamoDB (migration in progress) ----
# The table name is fixed by CloudFormation in production. Locally, point
# DYNAMODB_ENDPOINT_URL at DynamoDB Local (`docker compose up -d dynamodb` from the repo
# root) and the store package will use it. NEVER set the endpoint in production.
# DDB_TABLE=dakkamotors
# DYNAMODB_ENDPOINT_URL=http://localhost:8123
