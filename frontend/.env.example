# API root. Vite proxies /api to Django in dev, CloudFront routes it in production.
VITE_API_BASE_URL=/api

# Number behind the Call Us button. Digits, spaces and dashes are fine —
# the tel: link is normalised to E.164 automatically.
VITE_CONTACT_PHONE=080-9282-3601
